server:
  port: 8080

spring:
  datasource:
    url: "${ACP_POSTGRES:jdbc:postgresql://localhost:5432/acp}"
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
  session:
    store-type: none

acp:
  sid: "${ACP_SID:s2891348}"
  s3-endpoint: "${ACP_S3:http://localhost:4566}"
  dynamodb-endpoint: "${ACP_DYNAMODB:http://localhost:4566}"
